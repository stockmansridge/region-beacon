# Fit the Add Prize window to the screen

## Changes
- Limit the Add/Edit Prize window height to the visible browser area.
- Make the prize fields scroll inside the window while keeping the title and Save/Cancel actions accessible.
- Preserve the existing prize fields, styling, and save behaviour.

## Verification
- Check the window at desktop and smaller mobile viewport heights at normal browser zoom.
- Confirm the full form can be scrolled and both action buttons remain usable.
